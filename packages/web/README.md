# @selene/web

Web frontend for Selene.

```
$ npm install
# Manually delete the react and react-dom packages from /node_modules in this package
# This is a hacky workaround to conflicting React versions (breaking hooks)
# with the top level Lerna packages
# A better fix will be developed eventually
$ npm run start
# Available at http://localhost:3000
```
