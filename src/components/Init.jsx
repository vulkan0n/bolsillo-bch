/* = Init =
 * 1. check if wallet already exists
 *  a. if no wallet, create it, then go to Main view
 *  b. if wallet exists, go straight to Main view
 */

function Init() {
  return (
    <div>Initializing Selene Wallet</div>
  )
}

export default Init;
